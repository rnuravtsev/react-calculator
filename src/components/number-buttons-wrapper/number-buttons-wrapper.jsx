import styled from "styled-components";

const NumberButtonsWrapper = styled.div`
  grid-area: number;
  display: grid;
  grid-gap: 10px;
  grid-template-columns: repeat(3, 1fr);
`;

export default NumberButtonsWrapper;
